#include <criterion/criterion.h>
#include <stdlib.h>
#include <unistd.h>

#include "../src/npeg.h"

Test(img_reading, test_pbm_img_reading) {
    // ImgResult res = img_read("../tests/imgs/ascii/small_sample1.pbm");
    // Img *img = (Img *)res.data;
    // img_write(img, "res1.pbm");
    // img_free(img);
}
